import Star from "../objects/star"
import Player from "../objects/player"

const propConfig = {
    "star": Star,
}

const playerConfig = {
    "player": Player
}

export { propConfig, playerConfig };